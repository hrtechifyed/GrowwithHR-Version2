# GrowWithHR v0.19.0 — M4 Explainable Intelligence

Status: **released**  
Release date: **2026-07-27**  
Private intelligence route: `/m4-explainable-intelligence.html`  
Stable public production route: unchanged  
Canonical package version: `0.19.0`  
Cloud persistence: not introduced

## Purpose

M4 converts the validated M2 traceability and M3 Compliance Story contracts into separate, explainable intelligence dimensions. It deliberately avoids a single blended percentage.

The release provides:

1. **Profile completeness** — weighted completion of governed organisation facts.
2. **Applicability certainty** — deterministic rule outcomes with supported statuses and no missing decision facts.
3. **Evidence coverage** — governed findings or facts with an explicit available or verified evidence state.
4. **Information-gain questions** — unanswered governed questions ranked by the named dimension they improve.
5. **Reproducible decision trace** — a stable record of each calculation and why it was performed.

## Safe-intelligence rules

- No blended health, maturity or compliance score is produced.
- Applicability, completeness and evidence remain separate.
- The model is deterministic for the same input; only `generatedAt` changes.
- The private view reads protected browser records without writing to them.
- No cloud persistence, authentication or evidence upload is added.
- The result is not legal certification.
- Compliance interpretation remains restricted to India operations.

## Implementation and integration record

| Pull request | Result |
| --- | --- |
| #64 | Deterministic M4 model, schema, private route, information-gain ranking, decision trace and dedicated checks. |
| #65 | Governed law transparency and M4 output integrated into the executive PDF and email-delivery path. |
| #66-#70 | Report ordering, legal links, industry questions, working-model locks and assessment reliability corrected. |
| #71-#72 | Assessment inputs and report sequencing rebuilt around a founder-first narrative and final acceptance rules. |
| #73 | Maintained browser-delivery adapter timeout fixed. |
| #74 | Historical stacked PDF layers replaced with one single-tier Light/Dark assembler and optional annexure. |
| #75 | Program status reconciled with the completed M4 and report-hardening implementation. |

## Acceptance evidence

The release demonstrates:

- three separate metrics constrained to 0–100;
- no `overallScore`, `overall` metric or blended percentage;
- deterministic output for the same protected input;
- targeted questions ranked by estimated information gain;
- a minimum five-step decision trace;
- a JSON Schema 2020-12 contract;
- no writes to protected storage;
- no change to the stable public route or persistence posture;
- founder-facing report and delivery integration;
- one controlled Light and Dark PDF structure with an optional detailed annexure.

## Validation commands

```bash
node --check js/assessment-v3/explainable-intelligence-model.js
node tests/m4-explainable-intelligence-contract-checks.mjs
node tests/m4-law-transparency-combination-checks.mjs
node tests/m4-report-integration-checks.mjs
npm run test:release
```

## Protected boundaries

- `/analyze-company.html` remains the stable public route.
- `/analyze-company-v3.html` and `/m4-explainable-intelligence.html` remain private validation routes.
- `complianceDnaV3` remains disabled by default.
- Protected browser-storage, report, PDF, email and delivery contracts remain compatible.
- No cloud persistence or storage migration is introduced.

## Rollback

Revert the coordinated `v0.19.0` release commit or disable the private M4/report integration layers. The stable v2 route remains available and no storage cleanup is required.
