# GrowWithHR v0.19.0 — M4 Explainable Intelligence

Status: **implementation complete; coordinated release cut pending**  
Private intelligence route: `/m4-explainable-intelligence.html`  
Stable public production route: unchanged  
Canonical package version: `0.18.0`  
Cloud persistence: not introduced

## Purpose

M4 converts the validated M2 traceability and M3 Compliance Story contracts into separate, explainable intelligence dimensions. It deliberately avoids a single blended percentage.

The implementation provides:

1. **Profile completeness** — the weighted completion of governed organisation facts.
2. **Applicability certainty** — the share of deterministic rule outcomes with supported statuses and no missing decision facts.
3. **Evidence coverage** — the share of governed findings or facts with an explicit available or verified evidence state.
4. **Information-gain questions** — unanswered governed questions ranked by the improvement they are expected to make to one named dimension.
5. **Reproducible decision trace** — a stable record of each calculation and why it was performed.

## Safe-intelligence rules

- No blended health, maturity or compliance score is produced.
- Applicability, completeness and evidence remain separate.
- The model is deterministic for the same input; only `generatedAt` changes.
- The private view reads protected browser records without writing to them.
- No cloud persistence, authentication or evidence upload is added.
- The result is not legal certification.
- Compliance interpretation remains restricted to India operations.

## Governed question catalogue

The M4 catalogue covers legal entity, employee count, Indian operating states or union territories, work model, People/HR ownership and evidence-register availability. Each question names the dimension it improves and carries a documented information-gain weight.

Subsequent merged work connected the governed M4 output to the founder-facing PDF and advisory-delivery path, while preserving the separation between applicability, evidence and missing information.

## Implementation and integration record

| Pull request | Result |
| --- | --- |
| #64 | Added the deterministic M4 model, schema, private route, information-gain ranking, decision trace and dedicated checks. |
| #65 | Integrated governed law transparency and M4 output into the executive PDF and email-delivery path. |
| #66-#70 | Corrected report ordering, legal links, industry questions, working-model locks and assessment reliability. |
| #71-#72 | Reworked assessment inputs and report sequencing around a founder-first narrative and final acceptance rules. |
| #73 | Fixed the maintained browser-delivery adapter timeout. |
| #74 | Replaced historical stacked PDF layers with one single-tier Light/Dark report assembler and optional annexure. |

## Core files

- `js/assessment-v3/explainable-intelligence-model.js`
- `schemas/explainable-intelligence.schema.json`
- `m4-explainable-intelligence.html`
- `tests/m4-explainable-intelligence-contract-checks.mjs`
- `.github/workflows/m4-explainable-intelligence.yml`
- `js/pdf-law-transparency-core.js`
- `js/report-acceptance-corrections.js`
- `js/report-runtime-bootstrap.js`

## Acceptance evidence delivered

The implementation demonstrates:

- three separate metrics, each constrained to 0–100;
- no `overallScore`, `overall` metric or blended percentage;
- deterministic output for the same protected input;
- targeted questions ranked by estimated information gain;
- a minimum five-step decision trace;
- a schema contract using JSON Schema 2020-12;
- no writes to protected storage;
- no change to the stable public route or persistence posture;
- founder-facing report and delivery integration through the current report runtime;
- one controlled Light and Dark PDF structure with an optional detailed annexure.

## Validation commands

```bash
node --check js/assessment-v3/explainable-intelligence-model.js
node tests/m4-explainable-intelligence-contract-checks.mjs
node tests/m4-law-transparency-combination-checks.mjs
node tests/m4-report-integration-checks.mjs
npm run test:release
```

Relevant dedicated and maintained workflows must pass on the eventual release branch.

## Remaining release gates

M4 is implementation-complete but is not yet the canonical released application version. Before a coordinated `0.19.0` version cut:

1. complete a deployed assessment-to-PDF-to-email smoke test with the current Render and Gmail configuration;
2. verify and archive reply-to and internal-notification behaviour;
3. archive representative current Light, Dark and dual-delivery PDFs;
4. run the complete maintained release and browser validation on the release branch;
5. align package, application configuration, visible version labels, tests, roadmap, changelog and validation records to `0.19.0` in one focused release pull request.

## Release decision

The canonical application version remains `0.18.0`. The next release pull request should perform only the coordinated `v0.19.0` cut after the outstanding operational evidence is approved. Report presentation refinements or new product scope must not be bundled into that version-cut pull request.

The consolidated post-v0.18 implementation and remaining-gate record is maintained in `docs/releases/post-v0.18-m4-report-hardening-status-2026-07-27.md`.

## Rollback

Before the coordinated version cut, disable or revert the relevant private M4 or report-integration layer. M3 remains complete, the public v2 route remains available, and no storage migration or cleanup is required.
