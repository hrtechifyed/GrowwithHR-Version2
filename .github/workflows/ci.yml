name: GrowWithHR CI

on:
  push:
    branches:
      - main
      - "feature/**"
      - "fix/**"
      - "release/**"
      - "hotfix/**"
      - "agent/**"

  pull_request:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality-checks:
    name: Quality checks
    runs-on: ubuntu-latest

    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Check version consistency
        run: npm run version:check

      - name: Validate compliance data
        run: npm run validate:compliance

      - name: Run baseline contracts
        run: npm run test:baseline

      - name: Run Organization Intelligence v1 structured checks
        run: node tests/organization-intelligence-v1-checks.mjs

      - name: Run site shell first-paint checks
        run: node tests/site-shell-first-paint-checks.mjs

      - name: Run M1 architecture contracts
        run: npm run test:m1

      - name: Run M2 architecture contracts
        run: npm run test:m2

      - name: Run M3 architecture contracts
        run: npm run test:m3

      - name: Run client and server readiness checks
        run: >
          npm run test:client-readiness &&
          npm run test:server-cors &&
          npm run test:email-signature &&
          npm run test:footprint-priorities

      - name: Run presentation checks
        run: npm run test:presentation

      - name: Run report experience, intelligence and visual-section checks
        run: >
          npm run test:report-experience &&
          npm run test:report-intelligence &&
          npm run test:visual-story-report

      - name: Run journey and scenario regression checks
        run: >
          npm run test:static &&
          npm run test:frontend &&
          npm run test:journeys &&
          npm run test:input-combinations &&
          npm run test:report-scenarios &&
          npm run test:golden

      - name: Run archived UX compatibility checks
        run: npm run test:ux
