// Zero-cost prototype: account authentication is intentionally deferred.
// Keep this module so the shared site shell can import it without redirecting users.
export async function requireGrowWithHRAccount() {
  return null;
}
